## 2026-04-15

- Creé el skill `skills/computer-use-image-paste/` con `SKILL.md` y `references/acceptance-checklist.md` para sesiones con computer-use/clipboard real. El skill deja explícito que si la sesión no tiene computer-use no se debe fingir una prueba física.
- Implementé en el repo local de OpenClaw (`/Users/hectorvelasquez/proyectos/openclaw/openclaw`) soporte en el TUI para convertir paths locales pegados de imágenes en attachments reales antes de `chat.send`, en vez de depender de rescate posterior por path.
- Archivos clave del cambio TUI: `src/tui/tui-image-paste.ts`, `src/tui/tui-image-paste.test.ts`, y actualizaciones relacionadas en el flujo de envío/UI/documentación para mostrar adjuntos de imagen.
- Alcance real del fix del TUI: resuelve `cmd+v` cuando la terminal pega un path local válido de imagen; no resuelve todavía clipboard nativo con bytes reales de imagen estilo Claude Desktop.
- Se dejó commit del repo OpenClaw con hash `84326c893` y mensaje `tui: attach pasted image paths`.
- Durante el trabajo también quedó registrado commit en workspace para el skill: `58ae6e2` con mensaje `skill: add computer use image paste spec`.
- Cron `Fix DB Sequences (5AM daily)` ejecutado a las `2026-04-15 05:00 America/Mexico_City`: `scripts/fixes/fix_sequences_cron.js` terminó con `0 fixed, 127 ok, 2 skipped`; PostgreSQL conectó bien y no hizo falta corrección ni alerta por WhatsApp.
