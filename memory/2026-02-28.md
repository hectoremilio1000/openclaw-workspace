# 2026-02-28

## Roles/Permissions System — Full Implementation

### What was done
- Designed complete role/permission system for GrowthSuite POS
- Studied AWS IAM, Google Cloud IAM, GitHub Orgs, Auth0, SoftRestaurant
- Created proposal doc: `/Users/hectorvelasquez/proyectos/growthsuite/growthsuite-roles-proposal.md`
- Ran wizard (15 questions) with user to define exact permissions per role
- Implemented all 5 phases

### Phase 1-2 (Backend) — PUSHED to main
- 7 migrations in pos_auth_api (6 new tables/columns + 1 PK fix)
- 36 permissions, 180 role_permissions, 28 role_modules seeded
- Models updated: Role, Permission, RolePermission, User (authorizationPin), RoleModule, AuthorizationLog
- `GET /api/me` returns `{user, permissions, modules}` with restaurant override logic
- `POST /api/authorize` — PIN-based authorization + audit log
- `checkPermission()` middleware
- `GET /api/roles/:id/effective-permissions?restaurant_id=N`
- `PUT /api/roles/:id/permissions` — upsert with restaurant overrides
- Commits: `3e723e1f`, `d7c9fe4c`

### Phase 3-5 (Frontend) — LOCAL ONLY, NOT PUSHED
- AuthContext updated for new /api/me format (backward compatible)
- `usePermissions` hook: `can()`, `maxValue()`, `scope()`, `hasModule()`
- Dynamic sidebar in PrivateLayout.tsx (filters by role modules)
- `AuthorizationModal` component + `useAuthorize` hook
- `PermissionsPage` at `/hour_cut/permisos` (toggles, sliders, dropdowns per role)
- Custom role creation (clone from base)
- Commits in pos-front: `248726f`, `05325d1`, `9da2afd`

### Bugs found and fixed
1. role_permissions PK `(role_id, perm_code)` prevented restaurant overrides — changed to id PK + unique index
2. `loadPermissions` ORDER BY was wrong — NULLs last in Postgres ASC, fixed to `IS NOT NULL ASC`
3. Controllers missing `await user.load('role')` before checking `role.code`

### Key decisions from wizard
- Captain: no create orders, no split/merge, 30% discount max, can charge
- Cashier: NO admin access, only pos_cash
- Only owner authorizes with PIN (separate authorization_pin)
- Owner can edit system role permissions per restaurant (Settings → Permisos)
- Bot only used by owner, no permission checks
- One authorization = one action (not time-based)
- Formas de pago: no role restrictions

### User preference noted
- USER WANTS LOCAL TESTING FIRST, THEN PUSH. Do NOT push until explicitly told.
- Always test locally before reporting completion

## Roles/Permissions — Cloud Deploy & Tests (21:30 CST)
- Pushed pos-app (4 commits) and pos-front (5 commits) to main
- Railway auth API deployed successfully, migration 007 ran
- Cloud tests: owner 36 perms/14 mods ✅, manager 26 perms/11 mods ✅
- Browser E2E on Vercel: owner full sidebar, manager restricted sidebar ✅
- Created Chef (level 3) and Marketing (level 2) custom roles in local DB for testing
- Fixed AuthContext.tsx: removed hardcoded `allowedAdminRoles` list — now uses modules for access
- Sent roles/permissions document to Jampier (+51900266553) via WhatsApp
- Document saved at workspace/growthsuite-roles-document.md
