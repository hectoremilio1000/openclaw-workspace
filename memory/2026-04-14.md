- Se arrancó Fase A del runtime operacional de Fogo de Chão para soportar pruebas continuas del bot/cerebro con datos vivos en `prod`, `dev` y `local`.
- El objetivo acordado quedó claro: no tratar Fogo como “script que mete datos”, sino como `restaurant runtime + evaluator harness` para que el bot pueda responder sobre operación realista, inventario, asistencias y reportes.
- Se creó documentación durable en `knowledge/architecture/growthsuite/restaurant-runtime-simulator.md` con módulos objetivo: `scenario-planner`, `attendance-simulator`, `operations-simulator`, `inventory-simulator`, `consistency-verifier`, `bot-evaluator`.
- Se crearon módulos nuevos versionables en el workspace: `scripts/fogo-runtime/scenario-planner.mjs` y `scripts/fogo-runtime/runtime-artifacts.mjs`.
- `scenario-planner.mjs` define blueprints por fase (`open`, `lunch`, `afternoon`, `dinner`, `close`) y por entorno (`prod/dev/local`) con `expectedCovers`, `expectedSalesRange`, incidentes de asistencia, presión de inventario y riesgos operativos.
- `runtime-artifacts.mjs` crea tracker de corrida, acumula totales operativos y escribe artifacts JSON en `artifacts/fogo-runtime/` (`last-*.json` y archivos timestamped) para usar luego en validación/evaluación.
- El runner real `scripts/fogo-simulator.mjs` fue refactorizado localmente para importar esos módulos, construir `scenario plan` y emitir artifacts, pero ese archivo está en `.gitignore`, así que la modificación quedó local-only y no entró al commit.
- Se hizo commit de lo versionable con hash `b9a6d03` y mensaje `docs: add fogo runtime planning modules`.
- Próximo paso recomendado: Fase B, agregar `consistency-verifier` post-run para comparar esperado vs observado y dejar issues/score por entorno.
- Se ejecutó la fase `close` del runtime en `prod`, `dev` y `local`, generando artifacts `artifacts/fogo-runtime/last-{prod,dev,local}.json`.
- `prod` close: corrida consistente (`orders=6`, `revenue=29928`, `checkOuts=18`, `inventoryConsumptions=9`, `inventoryWaste=4`), pero el verificador diario marcó `12 open-order records attached to closed shifts` y `13 inventory items have negative cumulative balance`.
- `dev` close: corrida consistente (`orders=6`, `revenue=17568.2`, `checkOuts=13`, `inventoryConsumptions=8`, `inventoryWaste=1`), pero el verificador diario marcó `4 open-order records attached to closed shifts` y `20 inventory items have negative cumulative balance`.
- `local` close: no cerró turno en esa ejecución (`orders=0`, `revenue=0`, `shiftsClosed=0`), aunque el día quedó sano; solo quedaron warnings por `run below expected min` y `17 inventory items have negative cumulative balance`.
- Con esto quedó confirmado que el `consistency-verifier` ya está corriendo post-run y está detectando dos clases de hallazgos útiles: integridad de órdenes/turnos al cierre y balances negativos de inventario acumulado.
- Se auditó `pos_admin_front` en `/reports` y quedó claro que la página no es solo menú: `src/pages/Reports/index.tsx` usa `ReportMenu` + `ReportModalDispatcher`, y `src/App.tsx` sí enruta `/reports` a `ReportsPage`.
- En `src/pages/Reports/reporteTree.ts` se confirmó que `Gastos` es placeholder real: aparece como `Gastos (próximamente)` con `disabled: true`, y `src/pages/Reports/modals/GastosReport.tsx` solo muestra texto de “próximamente”.
- Se confirmó que el problema de reportes no está en `ReportMenu` ni en `ReportModalDispatcher`, sino dentro de los modales y/o en los backends que consumen.
- `AttendanceByEmployeeReportModal.tsx` sí está implementado y usa `apiAttendance` sobre `apiAuth` (`/users`, `/attendance/sessions`, `/attendance/late`).
- `SalesTotalsModal.tsx`, `VentasMeserosReportModal.tsx` y `ProductPrepTimesReportModal.tsx` sí están implementados, pero dependen de `apiOrder` / `pos_order_api` (`/reports/sales-totals`, `/reports/waiter-sales`, `/reports/product-prep-times`).
- Hallazgo operativo importante para el bug de `/reports`: asistencia va por AUTH service, mientras ventas/meseros/tiempos van por ORDER service. Si asistencia funciona y los demás reportes no, la causa más probable está en `pos_order_api`, datos/filtros vacíos o entorno local incompleto, no en el contenedor `/reports`.
- En `pos_admin_front`, `.env` local apunta a `localhost` (`VITE_API_URL_AUTH=http://localhost:3340/api`, `VITE_API_URL_ORDER=http://localhost:3341/api`, etc.), mientras `.env.dev` apunta a Railway dev. Esto explica por qué algunos reportes pueden fallar localmente aunque el flujo UI exista.
- Durante los heartbeats se corrigió una falsa alarma operacional: un check de las 15:00 reportó 404 en inventory/centro-control/reservaciones/website por validar `/` en vez de sus health endpoints esperados. A las 21:00 se confirmó salud correcta usando endpoints adecuados: 9/9 backends OK y PostgreSQL OK (`orders` últimas 24h = 163).
- Resultados durables de evaluación del bot en Fogo que quedaron visibles por cron: categoría J (ambigüedad) quedó muy débil por `429`, aclaración inconsistente y mal manejo de multi-intent; categoría K (financiero avanzado) tuvo `0/10` correctos por falta de ruta/intención financiera real; categoría L (cumplimiento legal México) tuvo `0/10` correctos por fallback excesivo y casi nulo valor práctico.
