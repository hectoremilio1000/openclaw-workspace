- Reviewed external PDF `bot-pipeline-desglose---94d9f17e-8710-4390-9e7d-23fca1545dd6.pdf` against real GrowthSuite POS code to answer future questions about the admin bot pipeline.
- Verified architecture match: `pos-front/pos_admin_front/src/components/BotAdminAssistant.tsx` -> `pos-app/pos_auth_api/app/controllers/admin_bot_controller.ts` -> `pos-app/pos_bot_api/app/controllers/bot_messages_controller.ts` -> `pos-app/pos_bot_api/app/bot/engine.ts`.
- Confirmed gateway tenant lock is real: `admin_bot_controller.ts` uses `auth.user.restaurantId` from JWT, sends that `restaurantId` to bot, and forces `provider: 'admin_panel'`.
- Confirmed admin-panel/WhatsApp conversation identity can mix when `restaurant.phone` exists because gateway sets `phone = restaurant.phone || admin-${restaurantId}-${user.id}`.
- Confirmed front behavior from code: `BotAdminAssistant.tsx` persists history in localStorage, persists `sessionId`, sends telemetry to `/bot/telemetry`, posts messages with `apiAuth.post("/bot/message", { text })`, and infers CTAs by keyword includes.
- Important code-vs-document delta: debounce is not ~800ms. In `pos-app/pos_bot_api/app/bot/queue/message_debounce.ts`, real values are `DEBOUNCE_MS = 2000`, `MAX_WAIT_MS = 5000`, `MAX_BATCH_SIZE = 5`, plus processing TTL cleanup.
- Important code-vs-document delta: dedup is persisted through `BotInboundDedup` in `receiveStage` keyed by `provider` + `providerMessageId`, not just a simple in-memory dedup.
- Important code-vs-document delta: `engine.ts` also calls `measureStage(...)` after classify, so the real flow is receive -> state(lazy) -> classify -> measure -> route -> execute -> persist -> reply.
- Working conclusion saved for future answers: the PDF is directionally correct, but not fully synced with current code. The biggest practical mismatch is the real 2s debounce, plus richer dedup and measurement details.
