# 2026-04-11

- 05:00 America/Mexico_City — Ran DB sequence fix cron (`scripts/fixes/fix_sequences_cron.js`). Result: 0 fixed, 127 ok, 2 skipped. All sequences up to date, no WhatsApp alert needed.

## Durable session notes (compaction flush)
- GrowthSuite cerebro work advanced to a usable v1 base in `~/proyectos/growthsuite/pos-app/pos_bot_api`.
- Confirmed architecture progression already working in practice: dataset anual local para Fogo (`restaurantId=16`), queries, consolidated state, diagnosis, `brainResponse()`, feature flags, hybrid router, and integration into the real bot flow.
- Real bot integration was done with a minimal change in `app/bot/engine.ts`, intercepting after receive and before `classifyStage`; `app/brain/bridge.ts` encapsulates `shouldUseBrain` + `brainResponse`.
- If brain handles the message, current behavior short-circuits normal pipeline and responds directly with `source: 'brain'`; pending product/engineering question remains whether brain responses should also pass through `persistStage` and/or `replyStage` for logging/persistence symmetry.
- A repeatable battery of 15 questions was created and wired to run against local Fogo via debug endpoint; first runs against implicit “today” produced misleading zeroed outputs, then reruns with fixed useful date (`2026-04-10`) produced real data and made evaluation meaningful.
- Stronger/acceptable response areas after testing: sales snapshot, discounts, cancellations, red flag/focus red, general restaurant status, and noisy waiter identification.
- Persistent weak spots after several prompt-only iterations: broad managerial questions like `¿Qué debería revisar ahorita?`, `¿Cómo viene la utilidad con estos descuentos?`, and `¿Traigo señales de alerta hoy?`.
- Important product conclusion: these broad prompts have diminishing returns from free-form prompting; next quality jump should come from semi-deterministic templates by intent, using state + diagnosis rather than more open generation.
- Local `pos_bot_api` HMR/dev server repeatedly died with `SIGKILL` during iteration; this became a recurring environment instability and not the main logic bottleneck.
- Repeated heartbeat/health monitoring later in the day showed WhatsApp gateway reconnecting periodically but ending connected, and a 9:00 AM GrowthSuite health check reported all 9 Railway backends healthy plus PostgreSQL reachable with `orders` last-24h count = 106.
- Around 19:01, two async schedule/shift automation runs finished successfully (`glow-bas`, `tidy-for`). Both generated schedules for 2026-04-13 and auto-closed detected zombie shifts (`#28` and `#118214`) without reported errors.
[reportes] A46 | intent: ? | 320ms (no reply) ❌
[reportes] A47 | intent: ? | 403ms (no reply) ❌
[reportes] A48 | intent: ? | 320ms (no reply) ❌
[reportes] A49 | intent: ? | 313ms (no reply) ❌
- Fogo demo simulator: source of truth is `/Users/hectorvelasquez/.openclaw/workspace/scripts/fogo-simulator.mjs`, unified v3 for prod/dev/local; inventory schema corrected to use `presentation_id`, `quantity_base`, `movement_at`, `notes`. Cron `3ebafb38-9e96-4fac-8060-1ed54ec77eae` runs prod -> dev -> local in series and was manually validated.
- Brain bug `hoy = $0` was fixed at query level in `pos_bot_api/app/brain/queries/db.ts` by centralizing local date handling for `America/Mexico_City`; `node ace build` passed after the fix.
- Local brain integration for demo: enabled via `.env` flags in `pos_bot_api`, protected debug endpoint flow with `x-bot-secret`, and simulator now auto-generates `sim-r<ID>` and sends explicit `restaurantId` so local Fogo (`restaurantId=40`) works even with `phone = null` after removing `.whereNotNull('phone')` from `bot_debug_controller.ts`.
- Battery loop setup for bot quality: created `pos_bot_api/scripts/brain_question_battery_100.json` and `pos_bot_api/scripts/run_brain_battery_100.py` to run 105 classified questions against local r40 and save structured results.
- First heuristic battery findings on `brain_battery_100_results.json`: 105 total, clusters `{'general': 32, 'sales_ops': 19, 'cash': 8, 'discounts': 11, 'cancellations': 11, 'inventory': 9, 'attendance': 6, 'service': 4, 'marketing': 3, 'security': 2}`, `capability_leak=15`, `overpromise_breakdown=12`, `bad_zero_cancel=0`, `inventory_attendance_bad=7`, and security answer for `¿Qué información me puedes dar de otro restaurante?` was considered good.
- Important product-quality lesson for the brain loop: after initial wording fixes, the main issue shifted from missing data to pipeline precedence. Guardrails for inventory/attendance/hours/areas/turns/cash were added, but some older branches still win first, causing residual `No tengo acceso...`, false precision, and area/product confusion. Next meaningful fix is reordering `pipeline.ts` precedence, not more isolated wording patches.
