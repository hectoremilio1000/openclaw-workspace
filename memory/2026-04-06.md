# 2026-04-06 — Memory Flush

## Cost optimization (Claude usage was at $44.13 / 18%)
Root cause: main session running in Opus 24/7 + auto-save checkpoints every 30min passing through main session (Opus) + benchmark rounds. Migrated everything automated to Codex 5.4:
- **Session Context Backup** → Codex 5.4 **isolated session** (no longer traverses main Opus session, 60s timeout)
- **Eliminated duplicate** "Claude Usage Report (4h)" cron
- **Bot Test Smoke** → Codex 5.4 (had been missed)
- All cron jobs now on Codex 5.4; Opus ONLY used when user explicitly picks it in main session
- Standing rule: Claude/Opus never runs automated workloads

## GitHub workspace sync repo
- Created private repo: `github.com/hectoremilio1000/openclaw-workspace`
- Initial push was DIRTY: included `pos_admin_front/.env.bak`, `.env.production`, memory files with Azure AD / Linear / Stripe / Postgres creds → triggered GitGuardian + GitHub secret alerts
- **FIX applied**: orphan commit + force push to wipe history. Repo now has 94 clean files only.
- `.gitignore` in workspace excludes all `pos_*` microservices, `.env*`, `node_modules`, screenshots, `pos_bot_simulator/.npm-cache`
- **TOOLS.md is NOT synced** (contains prod credentials) — stays local only
- Cron sync job: auto-commits + pushes every 2h using Codex 5.4, with secret scan pre-check that aborts if credentials detected
- Other compu setup: `git clone https://github.com/hectoremilio1000/openclaw-workspace.git ~/.openclaw/workspace` + pull cron

## Workspace inventory (what's portable)
Skills: screenshot, obsidian-1-0-0, deploy-verify, pos-bot-test-plan, softrestaurant-sync, model-benchmark
Root configs: AGENTS.md, SOUL.md, USER.md, HEARTBEAT.md, IDENTITY.md (TOOLS.md stays local)
Memory: model-playbook.md, codex-modes.md, codex-coaching-log.md, claude-code-knowledge.md, model-comparisons.md

## Fogo de Chão Santa Fe (restaurant_id = 40) — credentials generated
- **Pairing code:** `778899` (Caja, Comandero, Monitor)
- **Admin login:** `ricardo.mendes@fogodechao.com.mx` / `secret123` at pos-front-admin.vercel.app
- **Cash stations:** `FOGO-CAJA01` (Principal MASTER), `FOGO-CAJA02` (Bar DEPENDENT)
- **PIN scheme (inserted into `attendance_credentials`):**
  - Dueño Ricardo Mendes: `900001`
  - Gerentes (Carolina, Miguel): `800001`, `800002`
  - Cajeros (Daniela, Roberto, Ana Paula): `400001`, `400002`, `400003`
  - Meseros (13 total, João, María, Thiago, Alejandra, Rafael...): `500001` - `500013`
- All user passwords: `secret123`

## GrowthSuite bot architecture — user asked for Musk-style opinion
Current state diagnosed:
- `pos_bot_api` is a monolithic linear pipeline (receive→classify→route→execute)
- `classify.ts`: 521 lines of hardcoded keywords/regex (e.g. WAITER_MANUAL_KEYWORDS, howToPatterns)
- `execute.ts`: 1,199 lines
- 14 actions in a flat registry
- LLM used as a keyword router, NOT as an agent with real tool-calling
- Adding "marketing" or "reservations" under current arch = +500 lines classify + +800 lines execute per topic = Frankenstein path

My recommended architecture (5 layers):
1. Canonical metrics + event log (daily_metrics_by_restaurant materialized + append-only bot_events)
2. Decision Engine (SQL + rules + forecasts, no LLM)
3. Domain Agents (operations, commercial, inventory, customer, financial, marketing, reservations) — each = 1 class with `getCapabilities / getContext / decide / execute`
4. Orchestrator Agent — planner with real LLM tool-calling over a tools registry (like Claude Code pattern studied in `/Users/hectorvelasquez/Downloads/src`)
5. Daily Operator — proactive briefing ("what your restaurant needs today")

Proposed roadmap (12 weeks): Fase 1 event log + daily metrics + tool-calling refactor (kill classify.ts) → Fase 2 OperationsAgent (target Cat A 85-90/100, from 59.6) → Fase 3 CommercialAgent + Orchestrator + Daily Briefing v1 → Fase 4 Marketing + Reservations agents (now 1 file each).

**Pending user ask:** write `growthsuite-bot-architecture-v2.md` blueprint (1 dense page) with event schema, daily_metrics schema, tool registry refactor, DomainAgent interface, migration plan with feature flags, and first concrete PR (migrate cancel_product + apply_discount to new pattern). User has NOT yet approved writing it.

## Open items / watch
- WhatsApp gateway kept flapping today (status 408 multiple times around 13:52, reconnects as +5215521293811)
- POS bot Category A still 59.6/100; remediation not started
- OpenClaw 2026.4.2 update — status still unverified from prior session
